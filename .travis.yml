branches:
  only:
    - master
    - staging
language: node_js
node_js:
  - "7.1.0"
env: 
  - NODE_ENV=production
install:
  - npm -g install webpack
  - npm install
  - pip install --user -r requirements.txt
script: 
  - gulp css
  - npm run build
  - npm run lint
